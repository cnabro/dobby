import React, { PureComponent } from "react";

class Login extends PureComponent {
  render() {
    return (
      <React.Fragment>
          Login
      </React.Fragment>
    );
  }
}

export default Login;
