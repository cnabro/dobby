import React, { PureComponent } from "react";

class Member extends PureComponent {
  render() {
    return (
      <React.Fragment>
          Member
      </React.Fragment>
    );
  }
}

export default Member;
