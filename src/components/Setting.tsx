import React, { PureComponent } from "react";

class Setting extends PureComponent {
  render() {
    return (
      <React.Fragment>
          Setting
      </React.Fragment>
    );
  }
}

export default Setting;
